@page "/classes"
@attribute [Authorize]

<AddClassForm OnSubmit="Refresh" />
<ClassDetails Refresh="@refresh" />

@code {
    protected bool AddClassSubmitted = false;
    bool refresh = false;
    bool loading = true;

    void Refresh()
    {
        refresh = !refresh;
    }
}